# scrapy_crawler_template

## Build Docker Container

`$ docker build . -t scrapy`

## Run and Attach to Docker Container

`$ bash exec.sh bash`

## Create New Project (run above attach to enter docker container)

`$ scrapy startproject {PROJECT_NAME}`
